"""op CLI — promote, diff, validate.

Run via:
  python -m op_cli promote        # regenerate op.snapshot.json from op.json
  python -m op_cli diff           # show drift without promoting
  python -m op_cli validate       # check op.json is internally well-formed
"""
